# Audit Website KAMI Tree — 22 Julai 2026

## Skop dan semakan

- `node --check app.js` lulus.
- `node scripts/validate-data.js data.json` lulus: 68 ahli, 15 union. Satu rekod (`p45`) masih belum mempunyai tarikh lahir.
- Semakan graph data tidak menemui cycle atau rujukan pasangan/anak yang rosak.
- Semakan deployment sebelum housekeeping mengesahkan website dan `data.json` boleh dicapai secara public.

## Housekeeping dan pembaikan dibuat

- Import JSON kini melalui validator penuh sebelum UI atau storan browser diubah.
- Data cache browser yang tidak sah dibuang dengan selamat semasa dibaca.
- Reset data memuat turun serta mengesahkan data asal dahulu; data tempatan tidak dipadam jika rangkaian atau data asal gagal.
- Pemulihan paparan kosong hanya membuang state paparan yang rosak; cache data pengguna dikekalkan sehingga data asal berjaya dimuat turun dan disahkan.
- Fetch data asal juga menyemak respons HTTP dan struktur data sebelum menyimpan cache.
- `dataVersion` dikemas kini kepada `2026-07-22`.
- Dokumentasi aktif dipindahkan ke `docs/`, pelan Flutter lama diarkibkan, dan output ujian sementara dikeluarkan daripada Git.
- `.gitignore` dan `npm run check` ditambah untuk semakan yang boleh diulang.

## Risiko yang masih ada

1. PIN hanya mengunci antaramuka. Oleh sebab website statik menghantar `app.js` dan `data.json` kepada browser, ia bukan perlindungan server-side untuk data keluarga.
2. Screenshot audit dan gambar keluarga masih berada dalam repository. Jangan jadikan repo public sebelum data, aset dan sejarah Git ditinjau/diarkibkan secara berasingan.
3. Foto Home dan profil ialah JPEG besar. Penukaran kepada WebP/AVIF patut dibuat bersama semakan visual desktop dan mobile.
4. `app.js` dan CSS besar serta stabil. Pecahan modul/CSS hanya patut dibuat dengan regression test, bukan sebagai perubahan kosmetik besar.

## Baki tindakan yang memerlukan keputusan pemilik

- Sahkan tarikh lahir `p45` sebelum kemas kini data seterusnya.
- Pilih model privasi jika website perlu benar-benar dilindungi: data contoh sahaja atau backend/log masuk yang authenticated.
- Archive atau buang screenshot audit dan aset keluarga daripada Git selepas backup peribadi disahkan.
- Uji deployment terkini pada desktop, Android Chrome dan iPhone Safari.
